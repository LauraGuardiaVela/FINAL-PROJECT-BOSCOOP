using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProvaConjunta : MonoBehaviour
{
    public bool ConjuntaisWin;
    // Start is called before the first frame update
    void Start()
    {
        ConjuntaisWin = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
